行，我按你现在“在复习哈希 + 四层循环思路 + 去重没思路 + 空闲时间多”的情况，给你排一条**从哈希到双指针去重**的完整前置路线。你就照顺序刷，刷到后面 454 和 18 会自然“降维”。

---

## 第一阶段：哈希基础（你正在复习的主线）

目标：把“查补数”“计数”“去重集合”这三件事练熟

1. **1 两数之和**（哈希查补数的最基本模型）
2. **242 有效的字母异位词**（计数/频次 map 思维）
3. **349 两个数组的交集**（set 去重）
4. **350 两个数组的交集 II**（map 计数 + 输出重复次数）
5. **202 快乐数**（set 判环）
6. **383 赎金信**（计数 map 的应用）
7. **49 字母异位词分组**（哈希 key 设计能力）

> 做到：看到“次数/频次/重复”第一反应就是 map++ 或 set。

---

## 第二阶段：Two Sum 的“计数版”思想（直通 454）

目标：从“找一个解”升级到“统计方案数”，并学会“拆成两半”

8. **560 和为 K 的子数组**（前缀和 + 哈希计数：超级关键）
9. **974 和可被 K 整除的子数组**（同样是“计数 + 哈希”）
10. **454 四数相加 II**（最终目标）

> 454 的核心就是：
> AB 的所有和存到 map（次数）里，遍历 CD 的和，看 `-(c+d)` 出现多少次，加起来就是答案。
> 你做完 560/974 会非常顺。

---

## 第三阶段：排序 + 双指针基础（为 18 做准备）

目标：形成“双指针怎么动”的直觉

11. **167 两数之和 II（输入有序）**（最纯的双指针）
12. **26 删除有序数组中的重复项**（有序去重的基本功）
13. **88 合并两个有序数组**（指针移动熟练度）
14. **125 验证回文串**（双指针模板）

---

## 第四阶段：去重 & kSum 主线（直通 18）

目标：把“去重规则”刻进肌肉记忆（这就是你说的毫无思路的部分）

15. **15 三数之和**（必须做熟：排序 + 固定一个 + 双指针 + 去重）
16. **16 最接近的三数之和**（练双指针移动逻辑）
17. **18 四数之和**（最终目标）

> 你“去重没思路”，99% 就是 15 没打通。
> 15 打通后，18 只是“外面再多套一层固定”。

---

## 建议刷题节奏（你空闲多就按这个来）

* **第 1 天**：1, 242, 349, 350, 202
* **第 2 天**：383, 49, 560（重点啃透）
* **第 3 天**：974, 454（你会发现突然很顺）
* **第 4 天**：167, 26, 88, 125
* **第 5~6 天**：15（反复写到去重不出错）
* **第 7 天**：16, 18

---

## 你做题时的“通关标准”（简单但有效）

* **454**：你能明确说出“拆两半 + map 存次数 + 查补数”的流程，并写出 O(n²)。
* **15/18 去重**：你能说清楚两类去重：

  * 外层固定数去重：`i > 0 && nums[i]==nums[i-1] continue`
  * 内层双指针去重：找到解后 `l++/r--` 并跳过相同值

---

你先按这个列表刷。
刷到 **560/974** 或 **15** 的时候如果卡住（尤其是 15 的去重），你把你写的代码贴出来，我直接帮你指出“哪一行该怎么改”，让你一次性把去重逻辑打通。

