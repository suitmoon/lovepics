function occupy_host(ip, code, project = 'HOS') {
    $.ajax({
        url: '/hostsystem/occupy_host',
        method: 'post',
        data: {
            project: project,
            ip: ip,
            verification_code: code
        }
    })
}
function get_ve_code(ip)这里具体代码逻辑如下另外192.168.1.1是错误的示范ip
javascript:(function(){const ip=prompt("输入IP","192.168.1.1");if(!ip)return;fetch('/hostsystem/VerificationCode/get_code',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:'ip='+encodeURIComponent(ip)}).then(r=>r.json()).then(d=>alert(d.message)).catch(e=>alert('失败'))})();


我现在需要你写一个扩展程序 要求如下 
在原网页生成 一个小按钮 点击按钮后展开小网页 注意不要新开防止触发跨域限制
然后 小网页放置两个小标签 一个用来输入目标 ip  一个来输入 验证码 code  ，然后有一个按钮 获取验证码，点击按钮后触发get_verification_code(ip) 来获取验证码 ，验证码是在手机软件上，需要用户手动输入网页，点击确定验证码后锁定ip 验证码防止用户改动，然后 根据到具体时间 12:00、17:30触发 occupy_host，
比如早上则等到12点触发，下午1730触发，可以稍微延迟10-50ms，
project为默认不需要改动。增加一些相应提示，get_ve_code不需要，有验证码的话手机会提醒 
ip 和验证码锁定后增加个抢占倒计时即可 
