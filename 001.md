        rowStyle: function (row, index) {
            if (row['temporarily_offline']) {
                return {css: {"background": "#dddddd", "cursor": "no-drop"}}
            }
            return ''
        },
