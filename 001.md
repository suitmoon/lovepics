	for(int start = 0,end=nums.size()-1;start<=end;){
		int now = (start + end)/2;
		if(nums[now]==target)return now;
		else if(nums[now] > target){
			end = now -1;
		}else{
			start = now + 1; 
		}
	}
	return -1;
