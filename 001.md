	int front = 0,tail = nums.size()-1;
	while(front!=tail){
		int a = nums[front] ;
		int b = nums[tail];
		if(a*a > b*b){
		  nums[front] = b;
	  	  nums[tail] = a*a;
		  tail--;	  
		}else{
		  nums[tail] = b*b;
		  tail--;
		}
	}
	nums[front] = nums[front] * nums[front] ;
	return nums;
